# AGENTS.md – 레포지토리 운영 가이드

## 📌 목표
- 에이전트가 이 Maven 기반 Spring MVC 웹앱을 빠르게 설정·실행하도록 핵심 정보를 제공한다.
- 에이전트가 놓치기 쉬운 설정·명령어만 남기고, 일반적인 Java/Spring 설명은 제외한다.

---

## 핵심 런타임 및 빌드 환경
- 언어 및 JDK: Java 21 (최근 JDK 8에서 업데이트됨)
- 빌드 도구: Maven (war 패키징)
- WAS (Web Application Server): Apache Tomcat 11.0.21
- 운영체제: Windows (win32)
## 프레임워크 및 라이브러리 스택
- Spring Framework 7.0.7: 최신 버전의 Spring MVC 및 Spring JDBC 사용.
- MyBatis & MyBatis-Spring:
  - MyBatis 3.5.16 / MyBatis-Spring 3.0.3
  - Spring 7 버전과의 호환성을 위해 mybatis-spring 3.x 버전을 사용 중입니다.
- 데이터베이스: PostgreSQL (Connection Pool: Commons DBCP2)
- 뷰 엔진: JSP (JavaServer Pages)
- 기타:
  - JSON: Jackson (databind, jsr310)
  - Logging: Log4j2 & SLF4J

---

## 1️⃣ 프로젝트 구조
- `src/main/webapp` – 웹 애플리케이션 루트. `index.jsp` 가 여기 위치한다.
- `src/main/webapp/WEB-INF/dispatcher-servlet.xml` – Spring MVC 설정. `component-scan` 은 `com.demo01` 패키지를 스캔한다.
- `src/main/java/com/demo01/controller` – 컨트롤러 구현 위치 (`HomeController`).
- `src/main/resources/application.properties` – `PropertySourcesPlaceholderConfigurer` 가 로드하는 필수 파일 (비어도 존재해야 함).
- `pom.xml` – `war` 패키징, `spring-webmvc`·`spring-context` 의존성 포함.

---

## 2️⃣ 필수 명령어
| 목적 | 명령어 | 비고 |
|------|--------|------|
| 의존성·빌드 | `mvn clean package` | `target/demo01.war` 가 생성됨 |
| 로컬 Tomcat 실행 | `mvn tomcat7:run` *(Tomcat 플러그인 추가 시)* | 기본 포트 8080, `/` 경로는 `index.jsp` 로 포워드됨 |
| 테스트 실행 | `mvn test` | 현재 JUnit 3.8 사용 |
| 전체 청소 | `mvn clean` | `target/` 디렉터리 삭제 |

> **주의**: `application.properties` 파일이 없으면 컨텍스트 초기화가 실패한다 (`BeanInitializationException`). 반드시 프로젝트 루트 `src/main/resources` 에 존재해야 한다.

---

## 3️⃣ 웹 접근 흐름
1. `web.xml` 에서 `DispatcherServlet` 이 `/` 로 매핑됨.
2. `dispatcher-servlet.xml` 에서 `InternalResourceViewResolver` 가 `/WEB-INF/views/` 를 기본 프리픽스·`.jsp` 서픽스 로 설정한다.
3. `HomeController` 의 `@GetMapping("/")` 가 `forward:/index.jsp` 를 반환해 루트 요청을 `src/main/webapp/index.jsp` 로 포워드한다.

---

## 4️⃣ 흔히 놓치는 점
- **프로퍼티 파일**: 빈 파일이라도 `src/main/resources/application.properties` 가 존재해야 컨텍스트가 정상 초기화된다.
- **패키지 스캔**: `dispatcher-servlet.xml` 의 `component-scan` 은 `com.demo01` 로 제한돼 있다. 컨트롤러는 반드시 이 패키지(또는 하위)에 있어야 한다.
- **WAR 배포**: `mvn package` 로 만든 `.war` 를 외부 Tomcat에 배포할 경우 `WEB-INF/web.xml` 과 `dispatcher-servlet.xml` 위치가 유지돼야 한다.

---

## 5️⃣ 참고 파일
- `pom.xml` – Maven 설정 및 의존성.
- `src/main/webapp/WEB-INF/web.xml` – 서블릿·필터 정의.
- `src/main/webapp/WEB-INF/dispatcher-servlet.xml` – Spring 설정.
- `src/main/webapp/index.jsp` – 기본 페이지.
- `src/main/resources/application.properties` – 필수 빈 프로퍼티 파일.

---

## 기본규칙 정의 
나는 한글을 사용하는 초보개발자에요.
* 질문에 대한 답변은 한국어로 기본 표현해 주세요.
* 전문적인 용어는 영문으로 표시하고 옆에 "([힌글])" 형식으로 표현하세요.

## 1. 코딩 전 생각하기
추측하지 마세요. 혼란을 숨기지 마세요. 트레이드오프를 명확히 하세요.
구현하기 전에:

* 가정을 명시적으로 서술하세요. 불확실하다면 질문하세요.
* 여러 해석이 가능하다면 이를 제시하세요. 혼자 임의로 선택하지 마세요.
* 더 간단한 접근 방식이 있다면 제안하세요. 정당한 이유가 있다면 사용자 의견에 반론을 제기할 수도 있습니다.
* 불명확한 부분이 있다면 멈추세요. 무엇이 혼란스러운지 구체적으로 말하고 질문하세요.

## 2. 단순함 우선 (Simplicity First)
문제를 해결하는 최소한의 코드만 작성하세요. 추측성 코드는 금지입니다.

* 요청하지 않은 기능은 추가하지 마세요.
* 일회성 코드를 위해 추상화를 도입하지 마세요.
* 요청하지 않은 "유연성"이나 "설정 가능성"을 고려하지 마세요.
* 발생 가능성이 없는 상황에 대한 예외 처리를 하지 마세요.
* 50줄로 끝낼 수 있는 코드를 200줄로 썼다면 다시 작성하세요.

스스로 질문하세요: "시니어 엔지니어가 보기에 지나치게 복잡하다고 할까?" 만약 그렇다면 단순화하세요.
## 3. 외과적 수정 (Surgical Changes)
꼭 필요한 부분만 만지세요. 본인이 만든 쓰레기만 치우세요.
기존 코드를 수정할 때:

* 주변 코드, 주석 또는 포맷을 임의로 "개선"하지 마세요.
* 망가지지 않은 것을 리팩토링하지 마세요.
* 본인의 스타일과 다르더라도 기존 스타일을 따르세요.
* 관련 없는 사용되지 않는 코드(dead code)를 발견하면 언급만 하고 직접 삭제하지 마세요.

수정으로 인해 남겨진 것들:

* 본인의 수정으로 인해 더 이상 사용되지 않게 된 임포트, 변수, 함수는 제거하세요.
* 별도의 요청이 없다면 기존에 있던 사용되지 않는 코드는 제거하지 마세요.

테스트 방법: 모든 수정된 줄은 사용자의 요청과 직접적으로 연결되어야 합니다.
## 4. 목표 중심 실행
성공 기준을 정의하세요. 검증될 때까지 반복하세요.
작업을 검증 가능한 목표로 변환하세요:

* "유효성 검사 추가" → "유효하지 않은 입력에 대한 테스트를 작성하고, 이를 통과시키기"
* "버그 수정" → "버그를 재현하는 테스트를 작성하고, 이를 통과시키기"
* "X 리팩토링" → "작업 전후에 테스트가 통과하는지 확인하기"

단계가 많은 작업은 간단한 계획을 세우세요:

1. [단계] → 검증: [체크사항]
2. [단계] → 검증: [체크사항]

강력한 성공 기준이 있어야 독립적인 반복 작업이 가능합니다. "작동하게 만들기"와 같은 약한 기준은 끊임없는 확인 과정을 필요로 합니다.
## 5. 마침표 사용 (한국어 출력 시)
한국어 문장 끝에는 콜론(:) 대신 마침표(.)를 사용하세요.
사용자가 한국어로 질문하면 출력도 한국어로 합니다:

* 다음 줄에 목록이나 예시가 오더라도 문장을 :으로 끝내지 마세요.
* 영어 문서로 학습된 LLM은 한국어에서도 콜론을 쓰는 습관이 있습니다. 이를 주의하세요.
* 테스트: 모든 한국어 문장의 종결 어미는 ., ?, ! 중 하나여야 하며, :은 안 됩니다.
* 코드 내부, 키-값 쌍, 레이블 안에서는 콜론을 사용해도 괜찮습니다. 문장 종결용으로만 쓰지 마세요.

## 6. 한국어 파일 헤더 주석
모든 새 소스 파일의 첫 줄에는 해당 파일의 역할을 설명하는 한국어 주석 한 줄을 넣으세요.
새 파일을 생성할 때:

* TypeScript/JavaScript: // 사용자 인증 상태를 관리하는 Context Provider
* Python: # KIS API 호출을 비동기로 래핑하는 클라이언트
* SQL: -- 일별 집계 결과를 저장하는 머티리얼라이즈드 뷰
* 필수 지시어('use client', 'use server', shebang 등) 바로 아래에 배치하세요.
* 설정 파일(*.config.ts, package.json 등)은 건너뛰세요.

이유: 에이전트는 코드베이스 전체가 아닌 파일을 선택적으로 읽습니다. 한 줄의 한국어 헤더는 다음 세션(사람 혹은 에이전트)이 파일 전체를 다시 읽지 않고도 즉시 문맥을 파악하게 해줍니다.
## 7. 계획 + 체크리스트 + 문맥 기록 (Plan + Checklist + Context Notes)
중요한 작업을 시작하기 전, 세 가지 결과물을 먼저 만드세요. 이들 없이 코딩을 시작하지 마세요.

* 계획(Plan) — 무엇을 왜 만드는지.
* 체크리스트(checklist.md) — 체크박스 형태의 구체적인 작업들. 진행하며 체크하세요.
* 문맥 기록(context-notes.md) — 작업 중 내린 결정과 그 이유. 지속적으로 추가하세요.

사용자가 계획만 주고 코딩을 시작하라고 하면 멈추고 물으세요: "체크리스트와 문맥 기록을 먼저 만들까요?" 다음 세션에서 작업을 이어받을 때 모든 결정 과정을 다시 추론하지 않으려면 이 기록들이 필요합니다.
## 8. 완료 보고 전 테스트 실행
코드를 수정했다면 "완료"라고 말하기 전에 테스트를 실행하세요.

* npm test, pytest, cargo test 등 프로젝트에서 사용하는 도구로 테스트를 실행하세요.
* 테스트가 통과하면 결과를 보고하세요. 실패하면 수정하고 다시 실행하세요.
* 테스트 환경이 없다면 최소한 프로젝트가 빌드/컴파일되는지 확인하세요.
* 사용자가 "끝", "완료"라고 말하기 전에 선제적으로 테스트를 실행하세요.

이 단계는 LLM이 가장 자주 건너뛰는 단계입니다. 타협할 수 없는 규칙으로 삼으세요.
## 9. 의미 있는 커밋 (Semantic Commits)
하나의 논리적 변화가 완료되면 커밋하세요. 사용자가 요청할 때까지 기다리지 마세요.

* 테스트: "이 커밋을 한 문장으로 설명할 수 있는가?" 가능하다면 커밋하세요. 아니라면 여러 변화가 섞인 것이니 분리하세요.
* 좋은 예: "auth 미들웨어 추가". 나쁜 예: "auth 추가하고 UI도 고치고 버그도 수정" (3개로 나누세요).
* 관련 없는 수십 개의 수정을 쌓아두어 개별적으로 되돌릴 수 없는 상태를 만들지 마세요.
* 무의미한 커밋은 지양하되, 의미 있는 단위로 커밋하세요.

참고: 개인용 프로토타입이나 일회성 스크립트의 경우 속도가 느려진다면 느슨하게 그룹화해도 좋습니다. 핵심은 '되돌릴 수 있는 능력'이지 형식적인 절차가 아닙니다.
## 10. 추측하지 말고 에러를 읽으세요
실제 에러 메시지와 로그를 읽으세요. 기억 속의 패턴과 맞추려 하지 마세요.
실패가 발생했을 때:

* 전체 에러 메시지와 스택 트레이스를 읽으세요.
* 추측하지 말고 실제 로그 출력을 확인하세요.
* 원인을 확인하기 전에는 "일반적인 해결책"을 적용하지 마세요.
* 불명확하다면 print/log를 추가해 상태를 확인한 후 수정하세요.

*이 파일은 에이전트가 레포지토리를 빠르게 파악하고, 초기화·실행 중 흔히 발생하는 오류를 방지하도록 설계되었습니다.*